---
name: setup
description: Install the deepseek-statusline — copies statusline.js to ~/.claude/ and configures settings.local.json
---

Run the setup script to install the statusline:

```bash
node ${CLAUDE_PLUGIN_ROOT}/commands/setup.ts
```

Then tell the user to restart Claude Code.
